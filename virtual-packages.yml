subdirs:
  linux-64:
    packages:
      __cuda: '11.8'
      __cuda: '12.1'
  win-64:
    packages:
      #__cuda: '11.8' # Will be covered by the cudatoolkit install
      __cuda: '12.1'

# Load 12.1 last so it's primary
